Images are generated (as needed) into subdirectories:

  * bfs
  * dfs
  * dfs_recursive
  
These can be converted into an animated GIF using any of the freely available online services, such as https://ezgif.com/maker

The sample animated gif images were created in this way, using the sample code.